name: Disabled Reflow Image Audit

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "This workflow is disabled. Reflow naming cleanup is the current scope."
